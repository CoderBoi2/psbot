module.exports.run = (client, message, args) => {
    message.channel.send("backrooms is a **scary** game in **roblox** which looks like the **end** in **mincraft**.")
    }
    
    exports.name = "backroom"
